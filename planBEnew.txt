Context

 3 detail pages (news/[slug], announcements/[slug], procedures/[slug]) still use hardcoded mock data with // Mock data - sẽ được thay thế bằng API call comments. The API service layer 
  (src/lib/api.ts) is already fully set up with real backend endpoints. Need to wire them up.

 Additionally, HomeSidebar.tsx has hardcoded image URLs and phone number, and InfoBar.tsx has a hardcoded news link.

 API Mapping

 All three detail pages use articles in the backend CMS (news, announcements, procedures/guides are all articles in different categories):
 ┌──────────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
 │    Frontend page     │                                               API calls needed                                               │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ news/[slug]          │ articlesApi.getBySlug(slug) + articlesApi.getRelated(slug) + categoriesApi.list() + articlesApi.getLatest(8) │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ announcements/[slug] │ articlesApi.getBySlug(slug) + articlesApi.getRelated(slug) + articlesApi.getLatest(4)                        │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ procedures/[slug]    │ articlesApi.getBySlug(slug) + articlesApi.getRelated(slug) + articlesApi.getLatest(4)                        │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ HomeSidebar          │ bannersApi.getActive('sidebar') + organizationApi.get()                                                      │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ InfoBar              │ articlesApi.getLatest(1)                                                                                     │
 └──────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
 Changes

 1. src/app/news/[slug]/page.tsx

 - Convert to async server component with params prop
 - Remove all hardcoded data (newsDetail, relatedNews, categories, otherNews)
 - Call articlesApi.getBySlug(slug) for article detail
 - Call articlesApi.getRelated(slug, 3) for related articles sidebar
 - Call categoriesApi.list() for categories sidebar
 - Call articlesApi.getLatest(8) for "Bai viet khac" section
 - Map ArticleDetailResponse fields to template: published_at → date, content → HTML, featured_image → thumbnail
 - Handle 404 with notFound() from next/navigation

 2. src/app/announcements/[slug]/page.tsx

 - Convert to async server component with params prop
 - Remove all hardcoded data (announcementDetail, relatedAnnouncements, otherAnnouncements)
 - Call articlesApi.getBySlug(slug) for detail
 - Call articlesApi.getRelated(slug, 3) for related
 - Call articlesApi.getLatest(4) for "Thong bao khac"
 - Map ArticleDetailResponse → announcement template fields
 - Handle 404

 3. src/app/procedures/[slug]/page.tsx

 - Convert to async server component with params prop
 - Remove all hardcoded data (guideDetail, relatedGuides, otherGuides)
 - Call articlesApi.getBySlug(slug) for guide detail
 - Call articlesApi.getRelated(slug, 5) for related guides
 - Call articlesApi.getLatest(5) for other guides
 - Handle 404

 4. src/components/home/HomeSidebar.tsx

 - Replace hardcoded image URLs with bannersApi.getActive('sidebar') — but this is a child component receiving props, so pass banner data from page.tsx
 - Actually: keep receiving data via props (banners, organization). The hardcoded images/phone will be replaced when the homepage passes real banner data. Skip this for now — the      
 homepage already calls compositeApi.getHomepage().

 5. src/components/client/InfoBar.tsx

 - This is a client component. The hardcoded news link should ideally come from the latest article.
 - Convert ticker to receive latestArticle prop from parent, OR fetch latest article server-side.
 - Since InfoBar is used in the layout, pass data from layout or keep as-is for now.

 Focus: Files 1-3 are the priority. Files 4-5 have minor hardcoded data that will naturally resolve when real data flows through.

 Verification

 1. npx tsc --noEmit — no type errors
 2. npm run build — builds successfully
 3. Visit /news/[any-slug], /announcements/[any-slug], /procedures/[any-slug] — should fetch from real API


 Context

 3 detail pages (news/[slug], announcements/[slug], procedures/[slug]) still use hardcoded mock data with // Mock data - sẽ được thay thế bằng API call comments. The API service layer 
  (src/lib/api.ts) is already fully set up with real backend endpoints. Need to wire them up.

 Additionally, HomeSidebar.tsx has hardcoded image URLs and phone number, and InfoBar.tsx has a hardcoded news link.

 API Mapping

 All three detail pages use articles in the backend CMS (news, announcements, procedures/guides are all articles in different categories):
 ┌──────────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
 │    Frontend page     │                                               API calls needed                                               │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ news/[slug]          │ articlesApi.getBySlug(slug) + articlesApi.getRelated(slug) + categoriesApi.list() + articlesApi.getLatest(8) │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ announcements/[slug] │ articlesApi.getBySlug(slug) + articlesApi.getRelated(slug) + articlesApi.getLatest(4)                        │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ procedures/[slug]    │ articlesApi.getBySlug(slug) + articlesApi.getRelated(slug) + articlesApi.getLatest(4)                        │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ HomeSidebar          │ bannersApi.getActive('sidebar') + organizationApi.get()                                                      │
 ├──────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
 │ InfoBar              │ articlesApi.getLatest(1)                                                                                     │
 └──────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
 Changes

 1. src/app/news/[slug]/page.tsx

 - Convert to async server component with params prop
 - Remove all hardcoded data (newsDetail, relatedNews, categories, otherNews)
 - Call articlesApi.getBySlug(slug) for article detail
 - Call articlesApi.getRelated(slug, 3) for related articles sidebar
 - Call categoriesApi.list() for categories sidebar
 - Call articlesApi.getLatest(8) for "Bai viet khac" section
 - Map ArticleDetailResponse fields to template: published_at → date, content → HTML, featured_image → thumbnail
 - Handle 404 with notFound() from next/navigation

 2. src/app/announcements/[slug]/page.tsx

 - Convert to async server component with params prop
 - Remove all hardcoded data (announcementDetail, relatedAnnouncements, otherAnnouncements)
 - Call articlesApi.getBySlug(slug) for detail
 - Call articlesApi.getRelated(slug, 3) for related
 - Call articlesApi.getLatest(4) for "Thong bao khac"
 - Map ArticleDetailResponse → announcement template fields
 - Handle 404

 3. src/app/procedures/[slug]/page.tsx

 - Convert to async server component with params prop
 - Remove all hardcoded data (guideDetail, relatedGuides, otherGuides)
 - Call articlesApi.getBySlug(slug) for guide detail
 - Call articlesApi.getRelated(slug, 5) for related guides
 - Call articlesApi.getLatest(5) for other guides
 - Handle 404

 4. src/components/home/HomeSidebar.tsx

 - Replace hardcoded image URLs with bannersApi.getActive('sidebar') — but this is a child component receiving props, so pass banner data from page.tsx
 - Actually: keep receiving data via props (banners, organization). The hardcoded images/phone will be replaced when the homepage passes real banner data. Skip this for now — the      
 homepage already calls compositeApi.getHomepage().

 5. src/components/client/InfoBar.tsx

 - This is a client component. The hardcoded news link should ideally come from the latest article.
 - Convert ticker to receive latestArticle prop from parent, OR fetch latest article server-side.
 - Since InfoBar is used in the layout, pass data from layout or keep as-is for now.

 Focus: Files 1-3 are the priority. Files 4-5 have minor hardcoded data that will naturally resolve when real data flows through.

 Verification

 1. npx tsc --noEmit — no type errors
 2. npm run build — builds successfully
 3. Visit /news/[any-slug], /announcements/[any-slug], /procedures/[any-slug] — should fetch from real API